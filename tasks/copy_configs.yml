---
- name: Ensure Ghostty config directory exists
  ansible.builtin.file:
    path: "/home/{{ target_user }}/.config/ghostty"
    state: directory
    mode: '0755'
  become: true
  become_user: "{{ target_user }}"

- name: Setup Ghostty config
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/ghostty.config"
    dest: "/home/{{ target_user }}/.config/ghostty/config"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"

# Terra repo doesn't seem to include any ghostty themes ...
- name: Ensure Ghostty theme directory exists
  ansible.builtin.file:
    path: "/home/{{ target_user }}/.config/ghostty/themes"
    state: directory
    mode: '0755'
  become: true
  become_user: "{{ target_user }}"

- name: Setup Ghostty Adwaita theme
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/ghostty_Adwaita.theme"
    dest: "/home/{{ target_user }}/.config/ghostty/themes/Adwaita"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"

- name: Setup Ghostty Adwaita dark theme
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/ghostty_Adwaita_Dark.theme"
    dest: "/home/{{ target_user }}/.config/ghostty/themes/Adwaita\ Dark"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"

- name: Ensure VS Codum config directory exists
  ansible.builtin.file:
    path: "/home/{{ target_user }}/.config/VSCodium/User"
    state: directory
    mode: '0755'
  become: true
  become_user: "{{ target_user }}"

- name: Setup VS Codium config
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/vscodium.json"
    dest: "/home/{{ target_user }}/.config/VSCodium/User/settings.json"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"

# Something in the Terra product.json is fucked, this product.json is from paul carottys repo
# Fixes the OpenVSX Marketplace being unavailable
- name: Fix VS Codium product.json
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/codium_product.json"
    dest: "/usr/share/codium/resources/app/product.json"
    mode: '0644'

- name: Setup Vim config
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/vimrc"
    dest: "/home/{{ target_user }}/.vimrc"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"

- name: Setup starship config
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/starship.toml"
    dest: "/home/{{ target_user }}/.config/starship.toml"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"

- name: Setup Bashrc
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/bashrc"
    dest: "/home/{{ target_user }}/.bashrc"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"

- name: Ensure Helix config directory exists
  ansible.builtin.file:
    path: "/home/{{ target_user }}/.config/helix"
    state: directory
    mode: '0755'
  become: true
  become_user: "{{ target_user }}"

- name: Ensure Helix theme directory exists
  ansible.builtin.file:
    path: "/home/{{ target_user }}/.config/helix/theme"
    state: directory
    mode: '0755'
  become: true
  become_user: "{{ target_user }}"

- name: Setup Helix config
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/helix.toml"
    dest: "/home/{{ target_user }}/.config/helix/config.toml"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"

- name: Setup Helix language server config
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/helix_languages.toml"
    dest: "/home/{{ target_user }}/.config/helix/languages.toml"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"

- name: Setup Helix theme
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/fedora_workstation_setup/files/helix_snazzy_dark.toml"
    dest: "/home/{{ target_user }}/.config/helix/themes/snazzy-dark.toml"
    mode: '0644'
  become: true
  become_user: "{{ target_user }}"
